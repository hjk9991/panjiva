# credentials — never commit
.env
*.env

# data files — stay on server A, never in git
*.parquet
*.csv
*.dta
# exception: small result tables in output/ are versioned
!output/tables/*.csv

# logs and caches
*.log
__pycache__/
.ipynb_checkpoints/
