# 작업명: Panjiva–소유구조–CIQ재무 통합 매칭 DB (`trade_ownership_master`)

- 담당: 20251201(김영수)
- 시작: 2026-08-05
- 관련 지시: `shared memory\BECRS_Matching_Project\03_김영수_통합DB_업무지시서.md`
- 상태: 진행 중 (1주치 파일럿 완료, 본 구축 전 스키마 확정 대기)

## 기록 (최신이 위)

### 2026-08-12 (3차) — 산출물 3버전 체계화 (v1 신규 / v2 H1 / v3 리뷰), 전 결정 사용자 확정

**한 일**: 업무지시서 03 산출물을 v1/v2/v3 세 버전으로 정리하고, 모든 분석 결정을
WRDS 다운로드 폼처럼 단계별 질문-답으로 사용자가 직접 확정. 전 버전 2024 H1 통일
(wf2q L0 원천 공용 → 교차 대사 가능).

| 버전 | 내용 | 산출 |
|---|---|---|
| **v1** (신규) | 지시서 문언 그대로 — 선적 1행에 양측 관계분류+재무 8블록(USD) | `tom_v1_2024h1\` 7,237,772행×178열, 월별 6파일 |
| **v2** (H1 확장) | 선적 base(v1 공용) + 쌍·법인·집단×분기 패널 3종 | `tom_v2_2024h1\` 02: 249,698 / 03: 303,375 / 04: 263,287행 |
| **v3** (리뷰 반영) | within-firm L0~L4 — 결정 45개 전수 검토, 4건 변경 재빌드 | `within_firm_pilot_2q\` L3·통계 재생성 |

- **사용 스크립트**: `tom_v1_shipment_master_2024h1.py`(신규) ·
  `tom_v2_panels_2024h1.py`(신규) · `wf2q_30_build_panels.py`(수정)
- **결정 기록**: 각 폴더 `DECISIONS.md` (v1 17건 / v2 통일 4건 / v3 변경 4건+확인 다수)
- **버전맵**: `shared memory\BECRS_Matching_Project\03_산출물_버전맵.md` (용어집 포함, 미팅용)

**교차 대사 (전부 일치)**: 선적 7,237,772건(v1=v2=v3) · 양측 매칭 금액 319,307.3백만$(v2=v3 게이트).

**주요 결정** (전부 사용자 확정): 원천 L0 재사용 / unmatched 를 con·shp·both 로 세분 /
**self=그룹내부(intra=1)** / 재무는 법인+모회사 × 연간+분기, 계정 9종 합집합,
**도착일별 as-of(결산일 기준, 소급 2년)**, **전 계정 USD 환산**(결산일 환율, 원표시는
환율 컬럼으로 복원) / v3 재무 층 v1 표준 교체(모회사·연간 13.5%) / HS 배분 L2·L3 통일 /
kor_mnc_link 거래시점 UP 화 (실측 영향 477→475 pair-월).

**신규 실측**: 재무 커버리지(수입액 기준) 법인 4.9% vs 모회사 32.3% / 소급 나이 중위 1일·
1년 초과 8.3% / ciqExchangeRate 는 latestSnapFlag=1 필수(하루 다중 스냅) · priceClose =
1USD당 현지통화 / 수출 테이블에 frob 없음(적재항 100% 미국이라 불필요).

**다음**: PI 보고(버전맵 1장) · 01·02 산출물 나오면 소스 교체 · v3 이월 과제(§5) 본구축에서.

---

### 2026-08-12 (2차) — 리뷰 G1~G5 반영: 패널을 기업×분기로

**한 일**: 2차 검토(`2026-08-12_FIX2`)의 지적 5건 반영. 체크리스트 **21/21 통과**.

**G1 (핵심) — F1이 절반만 고쳐져 있었다.** `attach_financials()` 는 행별 기준시점으로 제대로
만들었는데, **호출부가 `period_start = 표본 시작일`을 넘기고 있었다.** 기간을 넓히면 2025년 선적에
2020-01-01 기준 재무가 붙는다 — 버그가 SQL 에서 pandas 로 자리만 옮긴 상태였다.
게다가 새로 넣은 검사(`fin_age_days` 음수 0 · 4년 초과 0)가 **이 상황을 통과시킨다**
(두 값이 같은 기준에서 나오므로 정의상 참). 지적이 정확했다.

→ 집계 3종의 **키에 `trade_quarter` 를 추가**하고, `period_start` 를 그 분기의 시작일로 바꿨다.
v0.2 §7의 "분기 단위 확장"이 동시에 해결된다.

실증 확인(2023-12-28~2024-01-04, 2개 분기 표본):

| 회사 | 분기 | 기준시점 | 붙은 회계기간 |
|---|---|---|---|
| Amazon.com | 2023Q4 | 2023-10-01 | FY2022 |
| Amazon.com | 2024Q1 | 2024-01-01 | **FY2023** |

두 분기 모두 재무가 붙은 794개사 중 **538개사가 분기별로 다른 회계기간**을 받았다.
1주 표본은 분기가 하나라 행 수 불변(01 254,873 / 02 25,930 / 03 49,289 / 04 42,430).
안전장치로 패널에 기간 차원이 없으면 중단하도록 했다.

**G2** — README §1 행수가 재생성본과 어긋나 있었다(손으로 관리한 탓). 숫자를 고치고,
`main()` 이 카탈로그 줄과 함께 **README 표도 출력**하도록 해서 복사만 하면 되게 했다.

**G3** — `03_통합스키마_v0.2.md` 를 코드에 맞춰 갱신: §4 에 결합 단위(패널 기간 시작일) 명시,
§1 행수 정정, 신규 컬럼 8종 계약 표 추가, 변경항목 #4 문구를 `merge_asof` 로, §5 에
`fin_perimeter_change` 항목 추가. **이 문서가 01·02 담당자와의 계약이라 코드와 어긋나면 안 된다.**

**G4** — `iff(x in (5,6,12), 1, 0)` 은 `x` 가 NULL 일 때 **0** 을 준다. "모름"이 "범위변경 없음"으로
기록되던 것을 `CASE WHEN ... IS NULL THEN NULL` 로 고쳐 결측을 보존(`Int64`).
`intra_group` 에서 미매칭을 결측으로 두는 원칙과 이제 일관된다.

**G5** — `build_group` 주석에서 `{con_up == shp_up}` = `parent_sub + sibling` 이라 단언한 부분에
`+ self` 를 더해 정확하게 고쳤다(self 를 별도로 뺀 것은 설계 의도, v0.2 미결 #9).

**부수**: `merge_asof` 의 두 시각 키 해상도(us vs ns)가 달라 나던 오류를 결합 직전 캐스팅으로 해결.

---

### 2026-08-12 — 코드 리뷰 지적사항 F1~F11 반영, 재추출

**한 일**: 검토 문서(`2026-08-11_FIX_trade-ownership-pilot.md`)의 지적 11건을 반영하고
1주치를 다시 뽑았다. 완료 체크리스트를 스크립트로 검증(`18/18 통과`).

- **사용/수정 스크립트**: `scripts\extraction\ex_20260805_trade_ownership_master_1week.py`
- **산출물**: `01` 254,873 / `02` 25,930 / `03` 49,289 / `04` 42,430행

**F1 (필수) — 재무 결합이 표본 전체에 단일 컷오프였다**
`periodEndDate < 표본시작일` 로 SQL 단계에서 회사당 1행을 잘라내고 있었다. 1주치에서는 차이가
없지만 기간을 2020~2025로 넓히면 **2025년 선적에 2020년 기준 재무가 붙는다**.
→ SQL 은 창(표본시작-4년 ~ 표본종료) 안의 **후보 전체**를 내려받고, 최종 선택은
`pd.merge_asof`(행별 기준시점, backward, 4년 tolerance, `allow_exact_matches=False`)로 옮겼다.
패널에 `period_start`/`period_end` 를 넣어 월·분기로 확장해도 같은 코드가 동작한다.
검증: `fin_age_days` 최소 1 · 최대 1,435(≤1,461), 음수 0건, 패널 행 수 불변.

**F2 — `build_pair` 가 `relationship` 을 `first` 로 뭉갬** → `n_relationship` 추가.
이번 표본에선 갈린 쌍 0건이지만, 다년 패널에서는 인수·분할이 사라지므로 검증에 상시 노출.

**F3 — `03_firm` 과 `04_group` 의 내부거래 비중 분모가 달랐다**
(03은 분류된 거래, 04는 미매칭 포함 총액 → 04가 기계적으로 낮게 나옴). `_value_split()` 한 곳으로
통일하고 원자료 5종(`_internal`·`_arms`·`_self`·`_unmatched`·`_classified`)을 함께 내보낸다.
검증: `classified + self + unmatched == value_usd` 오차 0, 두 패널의 내부거래 총액 일치.

**F4 — 진단 §5 커버리지가 실제 부착 규칙과 다른 창을 씀** → 동일 조건으로 맞추고,
리포트가 스스로 대사하도록 한 줄 추가(실측 4,758개 일치 확인).

**F5 — "Reclassified = 총액 동일"이 과했다.** 실측상 ~9%는 자산매각·중단사업 재분류라 손익
총액이 바뀐다. `ciqFinInstance.isRestatementTypeId`(`latestForFinancialPeriodFlag=1`, 기간당 최대 1행 확인)를
붙여 `fin_perimeter_change` 플래그 신설(이번 표본 1.6%). 소유구조로 거래를 분류하는 우리 설계에서는
**재무의 기업 경계와 관계분류 경계가 어긋나는 구간**이라 표시가 필요하다.

**F6~F8 (문서)** — `fin_year` → `fin_calendar_year` 개명(CIQ `calendarYear` 는 Compustat `fyear` 와
3·4·5월 결산에서 100% 불일치, 2~5월 결산 15.5%) · `ciqFinPeriod` 설명 정정(정정 전 원본을 담고
있지 않음, 컬럼 7종뿐) · `CLAUDE.md` 의 없는 경로(`C:\KDI\GVC\...`, `docs/` 접두어) 정리.

**F9~F11** — `_top_by_value` 안정 정렬(mergesort + 타이브레이크), 생성일 하드코딩 제거
(`date.today()`), 주석의 "3년 소급" → 4년, 미사용 인자 `trade_year` 제거,
README 에 `self` 가 현재 `intra_group` 에서 **제외**돼 있다는 기본값 명시.

**다음 할 일**: (변동 없음) PI 미결 #8~#10, 재무 통화 정규화, 분기 단위 확장.

---

### 2026-08-06 — 파일럿 재생성(04_group 추가) + 팀 공용 문서 반영

**한 일**: 파일럿을 검토하며 나온 결함 3건을 고쳐 통째로 재추출하고, 재사용 가능한 실측을
팀 공용 문서에 반영했다.

- **사용/수정 스크립트**: `scripts\extraction\ex_20260805_trade_ownership_master_1week.py`
- **산출물**: `C:\panjiva\data\staging\trade_ownership_pilot_1week\`
  (`01_shipment` 254,873 / `02_pair` 25,931 / `03_firm` 49,290 / **`04_group` 42,432**)
  — `_catalog.md` 갱신 완료

**고친 것 3건**

1. **재무 조회 대상에 최종 모회사 누락** — 무역 당사자만 조회해서, 미국에 직접 수입하지 않는
   해외 본사(삼성전자·Mazda 등)가 목록에서 빠졌다. 그 결과 자회사 재무가 전부 빈칸.
   → `ciqCompanyUltimateParent`로 모회사를 조회 대상에 포함.
   **재무가 덮는 수입액 6.0% → 42.9%.**
2. **재무 as-of 규칙이 미래정보를 흘림** — `calendarYear <= 거래연도`로 걸면 3월 선적에
   그해 12월 결산 재무가 붙는다. → `periodEndDate < 거래일`로 교체.
3. **`filingDate`를 시점 필터로 쓰라고 문서에 잘못 적음** — 이 컬럼은 최초 공시일이 아니라
   *최신 인스턴스* 제출일이라 나중 연차보고서의 비교 열로 갱신된다(결산일 대비 **중위 453일**,
   연간의 73.8%가 400~800일). 걸면 재무의 **80.9%가 소실**. → 사용 금지로 정정.
   최초 공시일이 정말 필요하면 `ciqFinInstance`에서 `restatementTypeId=2/1`을 **유형으로** 골라야 하며
   (`min(filingDate)`는 보도자료와 정식보고서를 뭉갬), 애초에 이 과제에는 필요 없다는 결론.

**새로 만든 것**: `04_group.parquet` — 한 행 = 기업집단. 소속 법인 무역 합산 + 모회사 재무 하나.
집단 내부거래(`imp_internal_share`)도 분리. **03과 04를 UNION하면 이중계상**이라 경고를 문서에 명시.

**추가 진단**: 크로스레퍼런스 오매칭 spot-check 목록(업종·품목 불일치 규칙, 1주 189건·3.65억 달러).
발단은 `Chevron Products Co.`(원유 수입) → `Chevron Inc`(Miller Industries 산하 **견인차 업체**) 오매칭.
CIQ 소유구조는 정상이고 `panjivaCompanyCrossRef`가 틀린 것으로 확인.

**팀 공용 문서 반영**

| 문서 | 추가 내용 |
|---|---|
| `DATA_GUIDE.md` | 함정 #17 `filingDate` · #18 재무는 상장 여부로 갈림 · #19 크로스레퍼런스 오매칭 / 레시피 **B4-1**(모회사 포함 조회) · **B7**(최초 공시일) |
| `data_dictionary.md` | PIT 구조 실측 · `restatementType` 5종 분포 · 인스턴스 계열 뷰(문서에 없지만 데이터 있음) · 미매칭 2단계 분해 |
| `CLAUDE.md` | 함정 §7 보강, §11(listagg·zfill) · §12(크로스레퍼런스) 신설 |
| `03_통합스키마_v0.2.md` | 신규 — v0.1 대체 |
| `91_질문_김영수…` | `TMPDIR` 환경 문제 추가 |

**검증**: 선적 254,873건(문서 실측치와 일치) · 키 중복 전부 0 · 네 파일 금액 합계가 기대값과 정확히 일치.

**다음 할 일 / 질문**

- [ ] PI: 주 산출물이 `02_pair`인지 `04_group`인지 (v0.2 미결 #8)
- [ ] PI: `self`(양측 동일 법인) 포함/제외 (#9), 집단 내부거래를 총계에 넣을지 (#10)
- [ ] 이경애: 재무 통화 — 원표시 vs USD 환산 (USD 27% 실측)
- [ ] 기간을 분기 단위로 확장 + 통화 정규화 레이어

---

### 2026-08-05 — 1주치 파일럿 추출 완료

**한 일**: 선적·거래쌍·기업 3개 층으로 합친 결과가 어떤 모양인지 확인하고,
미뤄뒀던 실측 항목 8가지를 한 번에 정리했다.

- **사용 데이터**: 없음 (Snowflake 직접 추출)
- **사용/수정 스크립트**: `scripts\extraction\ex_20260805_trade_ownership_master_1week.py` (신규)
- **산출물**: `C:\panjiva\data\staging\trade_ownership_pilot_1week\`
  (`01_shipment` 254,873행 / `02_pair` 25,927행 / `03_firm` 49,272행 + `90_checks.md` + `README_읽어보세요.md`)
  — `data\staging\_catalog.md` 기록 완료

**검증 (통과)**

| 항목 | 결과 |
|---|---|
| 표준 필터 후 선적 건수 | **254,873건 — `data_dictionary.md` 실측치와 정확히 일치** |
| 선적 중복 | 0건 (PIT 구간 조인이 행을 늘리지 않음) |
| 세 파일 금액 합계 | 기대값과 정확히 일치 (집계 누락·중복 없음) |

**확인한 것 8가지** (상세는 산출 폴더의 `90_checks.md`)

1. `ciqCompanyUltimateParentPIT` — `endDate` null 0건(전부 `9999-12-31` sentinel),
   **구간 중복 0건**(→ 선적 PK 안전), `dateTypeId` 값 1종뿐. 이력 2구간 이상인 회사는 3.4%.
2. PIT vs 스냅샷 — 수입자 기준 **6.9%**가 서로 다름(수출자 4.4%). **PIT를 기본으로 쓰는 근거 확보.**
3. 관계 분류 — `arms_length` 18.8% / `sibling` 2.1% / `parent_sub` 1.4% / `self` 0.2% /
   미매칭 77.4%(건수 기준).
4. family 크기 이상치 — 이 표본 최대 8,630(Bank of America). **펀드·트러스트는 무역 데이터에 안 나타남.**
5. 재무 커버리지 — 매칭 기업 49,272개 중 **5.0%만** 재무 보유. 표시 통화 **USD 20%**(CNY·JPY·INR·KRW 혼재).
6. 자식테이블 조인 부풀림 재현 — 행수 ×1.00인데 **TEU ×1.10**. 검증은 합계로 해야 함을 실측 확인.
7. 매칭률 — 수입자 51.1%(금액 69.0%) / 수출자 32.6%(46.7%) / **양측 22.6%(39.8%)**.
   문서 기록 범위(수입자 41~64%, 수출자 31~41%) 안.
8. 단가 분석 가능 여부 — HS6 셀 내 변동계수 중위 **0.54** → **단가 비교 가능**.

**부수 발견**

- `pull_panjiva_fixed.py`의 HS 재결합 로직 결함 2건을 실물로 확인.
  `listagg` 구분자 `'|'` + `hsCode` 문자열 정렬은 잘린 토큰을 복원하지 못한다
  (`rid=287952173`: 잘못된 방식 589개 vs 고친 방식 **593개** 인식).
  정렬 키는 자식 테이블의 `hsCodeId`, 구분자는 **빈 문자열**이어야 함.
  추가로 `hs6`에 `zfill(6)` 금지 — 4자리 HS가 `008528`이 되어 챕터가 뒤바뀐다(실측 121건 존재).
- 재무 as-of 규칙 — `calendarYear <= 거래연도`로 걸면 3월 선적에 그해 12월 결산이 붙어
  **미래 정보가 샌다**. `periodEndDate < 거래일` 로 걸어야 함.
- **환경 정정**: 91번 문서 6절의 "pyarrow 차단"·"`.snowflake.env` 없음"은 현재 사실이 아님.
  팀 공용 venv에서 pyarrow 25.0.0 정상, `%USERPROFILE%\.snowflake.env` 존재.
  (반대로 문서가 "정상"이라 적은 시스템 파이썬에는 패키지가 없다.)

**다음 할 일 / 질문**

- [ ] PI: 주 산출물이 `02_pair`(거래쌍)인지 `03_firm`(기업 패널)인지 확정 — 둘 다 같은 코드에서 나옴
- [ ] PI: 재무 통화 — 원표시 유지 vs `ciqExchangeRate`로 USD 환산 (초안 6절 미결 #3)
- [ ] 파일럿 결과 반영해 `03_통합스키마_v0.1_초안.md` → v0.2 개정
- [ ] `pull_panjiva_fixed.py` 결함 3건을 팀 표준본에 반영할지 판단 (+ 참조만 있고 없는 `REVIEW_A-server.md`)
- [ ] 91번 문서 6절 환경 항목 정정 (이경애 연구원 문서라 협의 필요)
